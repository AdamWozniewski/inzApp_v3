import { Response, Request } from 'express';

export function getIndividualSetting(req: Request, res: Response): Response {
    return res;
}

export function createIndividualSetting(req: Request, res: Response): Response {
    const {
    } = req.body;
    return res;
}

export function deleteIndividualSetting(req: Request, res: Response): Response {
    return res;
}

export function updateIndividualSetting(req: Request, res: Response): Response {
  return res;
}
const links = {
    endpointType: {
        markers: '/markers/',
        members: '/members/',
        orders: '/orders/',
        register: '/register/',
        login: '/login/',
    }
};

export default links;
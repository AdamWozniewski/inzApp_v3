const links = {
    endpointType: {
        markers: '/markers/',
        members: '/members/',
        orders: '/orders/',
        auth: '/auth',

        actions: {
            register: '/register',
            login: '/login',
        },
    }
};

export default links;